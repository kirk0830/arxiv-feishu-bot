# Update README.md (Logo + Fix Stale Examples) and Align Workflow Plan

## Summary

Add the project logo to README.md, fix remaining stale CLI examples, and
update the GitHub Actions workflow to use `lark-arxivbot` instead of
`python main.py` so that all entry points are consistent with the
documentation.

## Current State Analysis

- `README.md` (read from disk):
  - Missing the logo at the top.
  - Lines 241-245 contain a stale CLI example using repeated `--keyword`
    flags instead of the new expression syntax.
  - Lines 263-300 in "Customizing Search Queries" still show old Python
    f-string snippets that are no longer relevant since queries are now
    passed via CLI expressions.
- `.github/workflows/arxiv-daily-paper-push.yml`:
  - Step "Run arXiv bot" executes `pixi run python main.py`.
  - README says users should run `pixi run lark-arxivbot`; the workflow
    should do the same.

## Proposed Changes

### File: `/home/huangyike/devel/arxiv-feishu-bot/README.md`

**1. Add logo**
Insert the logo image right after the H1 title, before the description
paragraph:

```markdown
# arXiv Daily Paper Bot for Feishu (Lark)

<img src="docs/assets/arxivbot-logo.png" alt="arXiv Bot Logo" width="200">

> **Note**: The code in this repository was generated by the
> **Kimi-2.6-thinking** model.
```

**2. Fix stale CLI example under "Customizing Search Queries"**
Replace lines 241-245:

```markdown
```bash
pixi run lark-arxivbot \
  --category '"physics.bio-ph"||"cond-mat.stat-mech"' \
  --keyword '"force field"&&"coarse-grained"'
```
```

**3. Remove obsolete Python f-string examples**
Replace the entire block from "Example: Broaden to Biophysics..." through
"Example: Extend Date Range to Weekly Digest" with concise CLI-based
examples that use the expression syntax:

```markdown
#### Example: Broaden to Biophysics and Statistical Mechanics

```bash
pixi run lark-arxivbot \
  --category '"physics.chem-ph"||"cond-mat.mtrl-sci"||"physics.comp-ph"||"physics.bio-ph"||"cond-mat.stat-mech"'
```

#### Example: Add Force Field and Coarse-Grained Keywords

```bash
pixi run lark-arxivbot \
  --keyword '"molecular dynamics"&&("machine learning"||"deep learning"||"neural network"||"force field"||"coarse-grained")'
```

#### Example: Search Only Titles (Stricter)

Use the `ti:` prefix inside the expression. Note that this requires
manually constructing the query via the arXiv API directly, as the
expression parser uses `all:` by default for keywords.

#### Example: Extend Date Range to Weekly Digest

```bash
pixi run lark-arxivbot --days-back 7
```
```

### File: `/home/huangyike/devel/arxiv-feishu-bot/.github/workflows/arxiv-daily-paper-push.yml`

Change the run step from:

```yaml
run: pixi run python main.py
```

To:

```yaml
run: pixi run lark-arxivbot
```

## Assumptions & Decisions

- **Logo path**: Relative `docs/assets/arxivbot-logo.png` works because
  the repository root is the README rendering context.
- **Workflow alignment**: Using `lark-arxivbot` in CI ensures the exact
  same code path as local CLI usage, including the new default expression
  logic.

## Verification Steps

1. Review the rendered README.md preview to confirm the logo displays
   correctly.
2. Confirm all CLI examples use the expression syntax with `"..."` and
   `&&` / `||`.
3. Verify the workflow YAML syntax is valid (no indentation errors).
